services:
  postgres:
    image: pgvector/pgvector:pg16
    environment: { POSTGRES_USER: review, POSTGRES_PASSWORD: review_dev, POSTGRES_DB: review }
    ports: ["5432:5432"]
    volumes: [postgres-data:/var/lib/postgresql/data]
    healthcheck: { test: ["CMD-SHELL","pg_isready -U review"], interval: 5s, retries: 10 }
  api:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: postgresql://review:review_dev@postgres:5432/review

    depends_on:
      postgres: { condition: service_healthy }

    ports: ["8000:8000"]
    volumes: [./src:/app/src, ./data:/app/data]
volumes:
  postgres-data:
